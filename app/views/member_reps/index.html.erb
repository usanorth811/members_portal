    </div>
  </div>
</div>
</nav>
<p id="notice"><%= notice %></p>

<h1>Member Reps</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Phone</th>
      <th>Email</th>
      <th>Addr</th>
      <th>Company</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @member_reps.each do |member_rep| %>
      <tr>
        <td><%= member_rep.name %></td>
        <td><%= member_rep.phone %></td>
        <td><%= member_rep.email %></td>
        <td><%= member_rep.addr %></td>
        <td><%= member_rep.company %></td>
        <td><%= link_to 'Show', member_rep %></td>
        <td><%= link_to 'Edit', edit_member_rep_path(member_rep) %></td>
        <td><%= link_to 'Destroy', member_rep, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Member Rep', new_member_rep_path %>
