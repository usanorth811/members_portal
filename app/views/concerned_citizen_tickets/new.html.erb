<br/>
<div class="container">
  <div class="row">
    <div class="col-md-3"></div>
    <div class="col-md-6">
      <h1>New <%= params[:type] == 'cross_bore' ? 'Cross Bore' : (params[:type] == 'concerned_citizen' ? "Concerned Citizen" : '') %> Notification</h1>
      <%= turbo_frame_tag dom_id(@concerned_citizen_ticket) do %>
        <%=  render 'form', concerned_citizen_ticket: @concerned_citizen_ticket  %>
      <% end %>
    </div>
  </div>
</div>

